# FTP 客户端与服务器项目报告

## 一、项目架构与目录结构

本项目采用客户端 - 服务器（C/S）架构，基于 C 语言实现，严格遵循模块化设计原则，将网络通信、命令处理、数据传输等功能拆分为独立模块。项目目录结构如下：

```
FTP项目/
├── MyClient/                # 客户端程序
│   ├── cmd/                 # 命令处理模块
│   │   ├── client_cmds.c    # 实现命令逻辑
│   │   └── client_cmds.h
│   ├── net/                 # 网络通信模块
│   │   ├── client_socket.c  # 控制连接建立、命令发送、响应接收
│   │   └── client_socket.h
│   ├── transfer/            # 数据传输模块
│   │   ├── client_transfer.c # 文件上传/下载、目录列表接收
│   │   └── client_transfer.h
│   ├── ui/                  # 界面交互模块
│   │		├── ui_utils.c			 # 处理命令行输入与打印服务器返回信息
│   │		└── ui_utils.h
│   ├── utils/               # 工具函数模块
│   │		├── utils.c
│   │		└── utils.h
│   ├── Makefile             # 编译脚本
│   └── main.c               # 客户端入口
│
└── MyServer/                # 服务器程序
    ├── config/              # 配置模块
    │   ├── config.c         # 解析命令行参数、校验配置
    │   └── config.h
    ├── conn/                # 连接管理模块
    │   ├── client_conn.c    # 客户端连接创建、销毁、事件循环
    │   └── client_conn.h
    ├── net/                 # 网络通信模块
    │   ├── socket_utils.c   # 监听、接受连接、数据收发
    │   └── socket_utils.h
    ├── cmd/                 # 命令处理模块
    │   ├── ftp_cmds.c       # 处理客户端命令并返回响应
    │   └── ftp_cmds.h
    ├── transfer/            # 数据传输模块
    │		├── data_transfer.c	 # 文件上传/下载、目录列表发送
    │		└── data_transfer.h
    ├── utils/               # 工具函数模块
    │		├── utils.c
    │		└── utils.h
    ├── Makefile             # 编译脚本
    └── main.c               # 服务器入口
```

## 二、客户端模块实现

### 1. 网络通信模块（net/）

- **核心功能**：负责与服务器建立和管理控制连接，发送 FTP 命令并接收响应。
- **关键函数**：
  - `client_connect()`：通过 TCP 协议连接服务器控制端口（默认 21），返回控制连接文件描述符。
  - `client_send_cmd()`：自动为命令添加`\r\n`结尾并发送（符合 FTP 协议格式）。
  - `client_recv_resp()`：接收服务器响应并提取前 3 位状态码（如 220 表示连接成功）。
  - `client_listen_port()`：在主动模式（PORT）下创建本地监听 socket，等待服务器反向连接。

### 2. 命令处理模块（cmd/）

- **核心功能**：解析用户输入，转换为 FTP 协议命令并处理服务器响应。
- **关键命令实现**：
  - **PASV（被动模式）**：发送`PASV`命令后解析服务器返回的 IP 和端口，主动建立数据连接。
  - **PORT（主动模式）**：在本地指定端口监听，通过`PORT`命令通知服务器，等待其发起数据连接。
  - **RETR（文件下载）**：发送下载请求，通过数据连接接收文件并保存到本地。
  - **STOR（文件上传）**：发送上传请求，通过数据连接读取本地文件并发送至服务器。
  - **LIST（目录列表）**：获取服务器端目录内容，支持 CRLF 到 LF 的换行符转换。

### 3. 数据传输模块（transfer/）

- **核心功能**：处理文件和目录列表的数据传输细节。
- **关键实现**：
  - 使用 64KB 缓冲区高效读写数据，支持部分读写（`recv`/`send`返回值小于缓冲区大小）和信号中断重试。
  - `transfer_recv_file()`：从数据连接接收文件并写入本地磁盘（二进制模式）。
  - `transfer_send_file()`：读取本地文件并通过数据连接发送至服务器（二进制模式）。
  - `transfer_recv_list()`：接收目录列表并标准化换行符（可选），支持输出到终端或文件。

## 三、服务器模块实现

### 1. 配置模块（config/）

- **核心功能**：解析命令行参数（如监听端口、根目录），设置默认值并校验合法性。
- **关键函数**：
  - `config_init()`：初始化配置（默认端口 2121，根目录`./ftp_root`）。
  - `config_validate()`：检查根目录是否存在、端口是否在有效范围（1-65535）。

### 2. 网络通信模块（net/）

- **核心功能**：创建监听 socket，接受客户端连接，处理数据收发。
- **关键函数**：
  - `socket_create_listen()`：创建并绑定监听 socket，支持端口复用（`SO_REUSEADDR`）。
  - `socket_accept()`：接受客户端控制连接，返回控制连接文件描述符并记录客户端 IP 和端口。
  - `socket_send()`/`socket_recv()`：发送响应（自动添加`\r\n`）和接收命令（自动移除`\r\n`）。

### 3. 连接管理模块（conn/）

- **核心功能**：基于`select`实现多客户端并发处理，维护客户端连接状态。
- **关键实现**：
  - `client_conn_list`：动态管理客户端连接列表，支持扩容 / 缩容和空闲槽位复用。
  - `conn_manager_run()`：主事件循环，通过`select`监听监听 socket 和客户端连接的可读事件。
  - `handle_client_cmd()`：读取客户端命令并分发至命令处理模块，处理连接断开和退出逻辑。

### 4. 命令处理模块（cmd/）

- **核心功能**：解析客户端命令并执行对应操作（如认证、文件操作、目录切换）。
- **关键实现**：
  - 处理`USER`/`PASS`命令完成认证，支持匿名登录。
  - 响应`RETR`/`STOR`命令时建立数据连接，传输文件数据。
  - 处理`LIST`命令时生成目录列表并通过数据连接发送。

## 四、总结

本项目通过模块化设计实现了 FTP 协议的核心功能，客户端与服务器分别负责命令交互和数据传输的不同环节：

- 客户端专注于用户命令解析、数据连接建立和文件读写。
- 服务器专注于连接管理、命令执行和资源访问控制。

系统支持主动 / 被动两种数据传输模式，可稳定实现文件上传、下载和目录浏览。后续可扩展断点续传、权限管理等高级功能。